import { type } from "os";

let initialState = {};
type InitilState = typeof initialState;

const sideBarReducer = (state = initialState, action: any) => {
  return state;
};

export default sideBarReducer;
